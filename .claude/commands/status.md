---
description: Show live status of background executor(s), stalled PLANs, and context fullness for the current session. Reads .heartbeat/*.json, PLAN frontmatter, and the session transcript.
---

Run `python .claude/commands/lib/render_status.py` and display the output.
