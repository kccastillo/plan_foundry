---
description: Show live status of background executor(s) and stalled PLANs. Reads .heartbeat/*.json and PLAN frontmatter to display active execution and audit stalls.
---

Run `python "${CLAUDE_PLUGIN_ROOT:-plugins/plan-foundry-core}/commands/lib/render_status.py"` and display the output.
